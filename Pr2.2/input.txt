x := 5 + 3 * 2;
y := x - 4 / 2;
z := x + y;

if x - y then
  a := z * 2;
  if a - 10 then
    skip
  else
    b := a / 2 + x
  endif
else
  y := y + 1
endif;

while y - 10 do
  x := x + 1;
  if x - 15 then
    z := z + 1
  else
    skip
  endif
endwhile;

t := x + y - z
